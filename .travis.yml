language: go

go:
    - 1.7

sudo: required

before_install:
    - sudo add-apt-repository ppa:masterminds/glide -y
    - sudo apt-get update -q
    - sudo apt-get install glide -y

install:
    - glide up

script: glide nv | xargs go test

notifications:
    email: false
    slack: letsrock-today:eG4j11F8dlxEIPmoUq3m8bol
